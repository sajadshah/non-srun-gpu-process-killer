# non-srun-gpu-process-killer
This bash script monitor nvidia-smi and kills processes which actually use gpu but with no srun

Just put the file in somewhere and add it to crontab to run periodically.